#include <stdio.h>
#include <math.h>

int	main()
{
	 float a;
	 float b;

	 printf("a = ");
	 scanf("%f", &a);
	 printf("b = ");
	 scanf("%f", &b);
	 printf("Le produit de l'operation est %f", a*b);
	 return (a*b);
}
